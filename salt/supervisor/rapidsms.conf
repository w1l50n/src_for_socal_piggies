[program:rapidsms]
command = /opt/pi/rapidsms_pi/gunicorn_start.sh                      ; Command to start app
user = pi                                                ; User to run as
stdout_logfile = /opt/pi/rapidsms_pi/logs/gunicorn_supervisor.log    ; Where to write log messages
redirect_stderr = true                                         ; Save stderr in the same log

